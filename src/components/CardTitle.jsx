const CardTitle=({title})=>{
    return(
        <div className="shadow bg-white-500 pl-4 flex justify-begin items-center rounded-t">
        <h2 className="text-center">{title}</h2>
     </div>
    );
};

export default CardTitle;