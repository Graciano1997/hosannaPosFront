import { Link } from "react-router-dom"

const Navegation =()=>{
    return(
        <nav>
            <ul>
                <li><Link to={"/#"}/></li>
            </ul>
        </nav>
    )
};

export default Navegation;
